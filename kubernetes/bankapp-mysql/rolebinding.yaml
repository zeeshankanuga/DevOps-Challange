apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: bankapp-rolebinding
  namespace: mysql-ns

subjects:
- kind: ServiceAccount
  name: bankapp-sa
  namespace: mysql-ns

roleRef:
  kind: Role
  name: bankapp-role
  apiGroup: rbac.authorization.k8s.io
