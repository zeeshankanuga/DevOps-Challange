# 🕹️ Tic Tac Toe Web App (Containerized)

A simple Flask-based Tic Tac Toe web application ready for containerized deployment.

python3 -m venv my_env      # Create a virtual environment named 'my_env'
source my_env/bin/activate  # Activate the virtual environment (Linux/macOS)
pip install flask==3.0.3    # Install your package within the virtual environment
deactivate                  # Deactivate the virtual environment when done

---

## 📁 Project Structure
tic-tac-toe/
├── app.py
├── templates/
│ └── index.html
├── static/
│ └── style.css
├── requirements.txt
├── Dockerfile
└── README.md
